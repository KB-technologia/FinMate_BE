<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.finmate.member.mapper.MyPageMapper">

    <select id="findMyPageInfo" resultType="org.finmate.member.domain.MyPageVO">
        SELECT u.account_id                      AS accountId,
               u.email,
               DATE_FORMAT(ui.birth, '%Y-%m-%d') AS birth,
               ac.animal_name                    AS animalName,
               ac.animal_image_1                 AS animalImage
        FROM user u
                 JOIN user_info ui ON u.id = ui.user_id
                 LEFT JOIN animal_character ac ON ui.animal_id = ac.id
        WHERE u.id = #{userId}
    </select>
    <update id="updateUserWithPassword">
        UPDATE user
        SET password = #{password},
            email    = #{email}
        WHERE id = #{userId}
    </update>
    <update id="updateUserWithoutPassword">
        UPDATE user
        SET email = #{email}
        WHERE id = #{userId}
    </update>

    <update id="updatePasswordOnly">
        UPDATE user
        SET password = #{password}
        WHERE id = #{userId}
    </update>

    <update id="updateUserInfo">
        UPDATE user_info
        SET birth = #{birth}
        WHERE user_id = #{userId}
    </update>
</mapper>
